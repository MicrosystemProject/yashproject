server:
  port: 9002
spring:
  application:
    name: manageemp
  datasource:
    password: ''
    url: jdbc:mysql://localhost:3306/empdb?useSSL=false
    username: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        
